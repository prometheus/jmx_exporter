httpServer:
  threads:
    minimum: 1
    maximum: 10
    keepAliveTime: 120
  ssl:
    certificate:
      alias: localhost
  authentication:
    basic:
      username: Prometheus
      password: secret
rules:
  - pattern: ".*"
