httpServer:
  authentication:
    basic:
      username: Prometheus
      algorithm: environmentVariable
      passwordEnvironmentVariable: $SECRET
hostPort: application:9999
rules:
  - pattern: ".*"
