openTelemetry:
  preserveNames: true
  endpoint: http://prometheus:9090/api/v1/otlp
  protocol: http/protobuf
  interval: 1
httpServer:
  ssl:
    certificate:
      alias: localhost
rules:
  - pattern: ".*"
